#########################
### PERSONAL SETTINGS ###
#########################

### PATH config
PATH=$HOME/bin:/usr/local/bin:$PATH


### ZSH config
export ZSH="/Users/danielle.osullivan/.oh-my-zsh"
ZSH_THEME="agnoster"
DISABLE_UPDATE_PROMPT="true"
export UPDATE_ZSH_DAYS=13
COMPLETION_WAITING_DOTS="true"
plugins=(
  git
  zsh-syntax-highlighting
  zsh-autosuggestions
)
source $ZSH/oh-my-zsh.sh


### CUSTOM PROMPT config
prompt_context() {
  if [[ "$USERNAME" != "$DEFAULT_USER" || -n "$SSH_CLIENT" ]]; then
    prompt_segment black default "%F{red}%B[ %* ]%b%f %(!.%{%F{yellow}%}.) 🌸 %n 🌸 "
  fi
}


### PYENV config
# export PYENV_ROOT=/usr/local/var/homebrew/linked/pyenv/
# if which pyenv > /dev/null; then eval "$(pyenv init -)"; fi
# if which pyenv > /dev/null; then eval "$(pyenv virtualenv-init -)"; fi

### NVM config
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion


### ALIASES
alias gs="git status"
alias gb="git branch"
alias gch="gf && git checkout"
alias gl="git log --oneline --graph"
alias gc="git commit --no-verify"
alias ga="git add ."
alias gf="git fetch"
alias gfp="gf && git pull"

alias sourcezsh="source ~/.zshrc"
alias openzsh="open ~/.zshrc"

alias disable="sudo spctl --master-disable"
alias enable="sudo spctl --master-enable"

alias ssh_clear="ssh-add -D"
alias ssh_personal="ssh_clear; ssh-add ~/.ssh/id_ed25519_personal"




######################
### GRAZE SETTINGS ###
######################

### ALIASES
alias ssh_graze="ssh_clear; ssh-add ~/.ssh/id_ed25519_graze"
alias graze="cd ~/graze/"
alias ukstart="cd ~/graze/web; make build-web start-web; make dev-husk"
alias eustart="cd ~/graze/graze-subscription-app-frontend; yarn cache clean; yarn install; yarn run develop"
alias ukshopstart="shopify logout; shopify login; make start-dev-uk"
alias pistachio="cd ~/graze/pistachio; make install build start"

### DOCKER KILL CONTAINERS FUNCTION
function ukrestart {
  docker container kill $(docker ps -q);
  echo y | docker system prune;
  echo y | docker volume prune;
  ukstart
}

### DEPLOYMENT ALIASES
alias eurelease="git tag release-$(date -u +%Y%m%d-%H%M) && git push origin --tags"
alias shoprelease="eurelease"
alias euprerelease="git tag prerelease-$(date -u +%Y%m%d-%H%M) && git push origin --tags"
alias ukrelease="git tag web-$(date -u +%Y%m%d-%H%M) && git push origin --tags"


### OTHER CONFIG
export DEVELOPER_EMAIL=danielle.osullivan@graze.com


### ON RESTART
ssh_graze


### NEW SETTINGS APPENDED BELOW:
